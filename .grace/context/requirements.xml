<GraceRequirements graceVersion="4.0">
  <Applicability>voice-1c-mvp</Applicability>
  <ProjectName>voice-1c-mvp</ProjectName>

  <Annotation>
    Локальный голосовой ассистент для работы с данными 1С:
    распознавание русской речи, определение намерения,
    проверка остатков, подбор запчастей для техники,
    создание документов обеспечения (ЗаказНаРемонт, перемещения,
    заказы поставщику) и голосовой ответ.
  </Annotation>

  <Keywords>
    voice, 1c, stt, tts, llm, fastapi, mcp, inventory, parts, scenario-frame, erp
  </Keywords>

  <Goals>
    <Goal>Поддерживать полный голосовой цикл audio -> STT -> orchestration -> 1C -> TTS.</Goal>
    <Goal>Получать складские факты только из 1С или явно обозначенного mock backend.</Goal>
    <Goal>Вести бизнес-сценарии (STOCK_QUERY, REPAIR_ORDER) как персистентные
      ScenarioFrame на бэкенде: поля, коллекции, зависимости, pending-подтверждения.</Goal>
    <Goal>Разрешать ссылочные поля 1С строго: mention -> EntityResolver -> точный EntityRef.</Goal>
    <Goal>Формировать один ЗаказНаРемонт со всеми позициями корзины и документы источников обеспечения.</Goal>
  </Goals>

  <Constraints>
    <Constraint>LLM не является источником складских данных и не хранит бизнес-состояние сценария.</Constraint>
    <Constraint>Бизнес-состояние сценария — ScenarioFrame на бэкенде; история чата — только разговорный контекст.</Constraint>
    <Constraint>Ссылочное поле считается заполненным только при точной идентичности объекта 1С (EntityRef); имя — не ссылка.</Constraint>
    <Constraint>Write-эффекты (создание документов) — только через PendingAction и явное подтверждение актуальной версии frame.</Constraint>
    <Constraint>Ошибки 1С не должны превращаться LLM в выдуманные успешные ответы.</Constraint>
    <Constraint>Mock fallback должен быть явно различим от результата реальной 1С (поле source).</Constraint>
    <Constraint>Озвучивается только текст; таблицы и ссылки — визуальные элементы веб-чата.</Constraint>
  </Constraints>
</GraceRequirements>
