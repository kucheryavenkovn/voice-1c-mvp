[pytest]
testpaths = tests
addopts = -ra
markers =
    ui: browser/UI tests (need `pip install -r requirements-ui.txt && playwright install chromium`; skipped in CI)
    contract: tests pinned to recorded 1C MCP Toolkit responses
filterwarnings =
    ignore::DeprecationWarning
